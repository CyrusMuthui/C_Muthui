#include <stdio.h>

/**
 *main - function to count even and odd numbers
 *Return: integer
 */
int main(void)
{
	int i, even = odd = 0
		;

	printf("Enter the number of numbers you want to enter: \n");
	scanf("%d", &number);
}