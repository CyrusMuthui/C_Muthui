#include <stdio.h>

/**
 *main - unary, binary and ternary operators
 *Return: True or False
 */
int main(void)
{
	int x = 10;
	int y = 11;

	y = ++x;
	printf("x is equal to %d \n", x);
	printf("y is equal to %d \n", y);
	return (0);
}
